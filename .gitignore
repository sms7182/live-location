# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env # If you create a .env file for local environment variables
*.log

# Docker
# Ignore Docker-related files that are generated
.dockerignore # This file should generally be committed if used for build context
# Ignore images generated by Docker (can be large)
*.img
*.tar
*.gz

# Ignore Docker Compose override files if you use them for local dev
docker-compose.override.yml

# Ignore build artifacts for Docker builds, if any (e.g., .dockerbuild)
.dockerbuild/

# IDE files (common examples, add more if you use other IDEs)
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.Trash/
Thumbs.db

# If you store actual data files locally (e.g., SQLite dbs, CSVs)
# /data/